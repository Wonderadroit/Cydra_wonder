// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.20;

import {Test} from "forge-std/Test.sol";
import {AlchemixAccessControlFixture} from "../../Target.sol";

/// Generated by CYDRA from H-AUTH-setWhitelist.
/// Security invariant: an arbitrary external caller must not mutate privileged state.
contract CydraAuthInvariantTestVulnerable is Test {
    AlchemixAccessControlFixture internal target;
    address internal attacker = address(0xBEEF);
    address internal account = address(0xCAFE);

    function setUp() public {
        target = new AlchemixAccessControlFixture();
    }

    function testUnauthorizedCallerCannotMutatePrivilegedState() public {
        assertFalse(target.whiteList(account));
        vm.expectRevert();
        vm.prank(attacker);
        target.setWhitelist(account, true);
        assertFalse(target.whiteList(account));
    }
}
